apiVersion: apps/v1
kind: Deployment
metadata:
  name: matcha
spec:
	replicas: 1
	selector:
		matchLabels:
			app: matcha
	template:
		metadata:
			labels:
				app: matcha
		spec:
			containers:
				- image: node:20.9.0-alpine
					name: node
					ports:
						- containerPort: 3001
